import 'package:flutter/material.dart';

class StrictBlockScreen extends StatelessWidget {
  const StrictBlockScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}